Requies libclang, python 2 and the clang python bindings.

Clang python bindings can be installed with
    $ pip install --user clang

Usage:
    $ python ompt-{dummy-tool,dummy-tool,tracing-print}.py <PATH TO ompt.h>